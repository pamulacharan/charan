<%@ page language="java" contentType="text/html; charset=ISO-8859-1" pageEncoding="ISO-8859-1" %>
<%@ page import="java.sql.*" %>
<%
    // -----------------------------------------------------------------
    // 1. Handle POST login submission
    // -----------------------------------------------------------------
    if ("POST".equalsIgnoreCase(request.getMethod())) {
        String username = request.getParameter("textName");
        String password = request.getParameter("textpwd");

        // Basic input validation
        if (username != null && !username.trim().isEmpty() &&
            password != null && !password.trim().isEmpty()) {

            // Database credentials – move these to a config file in production!
            String dbUrl  = "jdbc:oracle:thin:@SMTRND:1531:SMTRND";
            String dbUser = "Linus";
            String dbPass = "G4uh3yAW!";

            String sql = "SELECT * FROM sv_user WHERE USER_ID = ? AND PASSWORD = ?";

            try (Connection conn = DriverManager.getConnection(dbUrl, dbUser, dbPass);
                 PreparedStatement ps = conn.prepareStatement(sql)) {

                ps.setString(1, username);
                ps.setString(2, password);

                try (ResultSet rs = ps.executeQuery()) {
                    if (rs.next()) {
                        // Login success → redirect to success page
                        response.sendRedirect(request.getContextPath() + "/Succes.jsp");
                        return; // stop further processing
                    }
                    // else: login failed → will redirect to Error.jsp after try-catch
                }
            } catch (SQLException e) {
                // Log the error (check your server logs!)
                System.err.println("Login database error: " + e.getMessage());
                // Redirect to Error.jsp (or show a custom message)
                response.sendRedirect(request.getContextPath() + "/Error.jsp");
                return;
            }
        }

        // If we reach here, login failed (invalid credentials or empty input)
        response.sendRedirect(request.getContextPath() + "/Error.jsp");
        return;
    }
    // -----------------------------------------------------------------
    // 2. Display the login form (for GET requests)
    // -----------------------------------------------------------------
%>
<!DOCTYPE html>
<html>
<head>
    <meta charset="ISO-8859-1">
    <title>Login Page</title>
    <style>
        body {
            background: linear-gradient(to bottom, #ffcccc 0%, #ffffcc 50%, #ccffcc 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            font-family: Arial, sans-serif;
            margin: 0;
        }
        h2 {
            color: #333333;
            text-align: center;
            margin-bottom: 20px;
        }
        table {
            border-collapse: collapse;
            width: 300px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 6px;
        }
        table td {
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid #dddddd;
        }
        table td:first-child {
            font-weight: bold;
        }
        table tr:last-child td {
            border-bottom: none;
        }
        .button-container {
            text-align: center;
            margin-top: 25px;
        }
        .button-container input {
            margin-right: 20px;
        }
        input[type="text"], input[type="password"] {
            padding: 8px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        input[type="submit"], input[type="reset"] {
            padding: 8px 16px;
            border-radius: 4px;
            border: none;
            background-color: #57b1dc;
            color: white;
            cursor: pointer;
        }
        input[type="submit"]:hover, input[type="reset"]:hover {
            background-color: #4682b4;
        }
    </style>
</head>
<body>
    <div>
        <h2>User Login</h2>
        <form action="<%= request.getContextPath() %>/login.jsp" method="post">
            <table>
                <tr>
                    <td>Username</td>
                    <td><input type="text" name="textName" required></td>
                </tr>
                <tr>
                    <td>Password</td>
                    <td><input type="password" name="textpwd" required></td>
                </tr>
                <tr>
                    <td colspan="2" class="button-container">
                        <input type="submit" value="Login">
                        <input type="reset" value="Reset">
                    </td>
                </tr>
            </table>
        </form>
    </div>
</body>
</html>
