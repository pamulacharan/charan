static {
    // Fallback: hardcode credentials (ONLY FOR TESTING!)
    props.setProperty("db.url", "jdbc:oracle:thin:@//SMTRND:1531/SMTRND");
    props.setProperty("db.username", "Linus");
    props.setProperty("db.password", "G4uh3yAW!");
}
